// Example usage of uuid in JavaScript

// Import the 'v4' function from the uuid package
const { v4: uuidv4 } = require("uuid");

// Generate a new UUID using v4
const uuid = uuidv4();

console.log("Generated UUID:", uuid);
